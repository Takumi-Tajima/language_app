%h1.fs-2.mb-4 予約履歴
.row.justify-content-center
  .col-8
    %table.table
      %thead
        %tr
          %th レッスン名
          %th 言語
          %th 講師
          %th 開始時間
          %th 終了時間
      %tbody
        - @booking_histories.each do |booking_history|
          %tr
            %td= link_to booking_history.lesson_name, users_booking_path(booking_history)
            %td= booking_history.lesson_language
            %td= booking_history.instructor_name
            %td= l(booking_history.start_at)
            %td= l(booking_history.end_at)
