%h1.fs-2.mb-4 予約一覧

%table.table.table-hover
  %thead.table-dark
    %tr
      %th レッスン名
      %th 言語
      %th 開始時間
      %th 終了時間
  %tbody
    - @bookings.each do |booking|
      %tr
        %td= link_to booking.lesson_schedule.lesson.name, users_booking_path(booking)
        %td= booking.lesson_schedule.lesson.language_text
        %td= l(booking.lesson_schedule.start_at)
        %td= l(booking.lesson_schedule.end_at)
