= simple_form_for [:instructors, lesson] do |f|
  = render 'layouts/shared/error_messages', resource: lesson
  = f.input :language
  = f.input :name
  = f.input :description, input_html: { rows: 5 }
  = f.button :submit, class: 'btn-primary'
